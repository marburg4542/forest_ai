# Pinned to the versions this pipeline was developed and verified against.
# Nothing here needs a GPU.

# --- point cloud I/O ---
laspy[lazrs]==2.7.0

# --- numerics ---
numpy==2.4.1
scipy==1.18.0
scikit-learn==1.8.0
pandas==2.3.3

# --- figures ---
matplotlib==3.10.1       # the QC PNGs written to disk
plotly==6.5.2            # interactive figures, and the bundled plotly.min.js

# --- web interface (not needed for run_pipeline.py) ---
fastapi==0.128.0
uvicorn==0.40.0
python-multipart==0.0.32
